# Data dictionary — `ebt_synthetic.csv`

Generated by `src/ebt_generator.py`, `random_seed: 42`. 137,080 rows, 2,000
households, 599 terminals, 2026-01-01 to 2026-07-02 (183 days). See
[`EXPLAIN_ebt_generator.md`](../explain/EXPLAIN_ebt_generator.md) for how and
why. Every value below is read from the file, not from the generator code.

| Column | Type | Description | Observed values / range |
|---|---|---|---|
| `transaction_id` | string | Unique row ID, `TXN00000000`–`TXN00137079`, assigned after chronological sort | 137,080 unique |
| `household_id` | string | Cardholder household, `H000000`–`H001999` | 2,000 unique |
| `user_role` | string | `primary` (cardholder) or `secondary` (household member using the same card, N2) | primary 135,394 / secondary 1,686 |
| `event_type` | string | `purchase` or `balance_inquiry` (P3 probes, P8 PIN attempts, and 2% of ordinary primary trips) | purchase 134,298 / balance_inquiry 2,782 |
| `timestamp` | datetime | Transaction time, minute resolution | 2026-01-01 07:05 – 2026-07-02 23:41 |
| `amount` | float, USD | Transaction amount; 0.00 for balance inquiries | min 0.00, median 17.86, mean 27.15, max 708.36 |
| `terminal_id` | string | Store/terminal identifier, `T00000`–`T00599` | 599 of 600 used |
| `terminal_lat`, `terminal_lon` | float | Terminal coordinates (synthetic, not real geography) | — |
| `terminal_state` | string | One of three synthetic hub regions | `State_A`, `State_B`, `State_C` |
| `home_lat`, `home_lon` | float | Household's registered home coordinates | — |
| `home_state` | string | Household's home hub region | `State_A`, `State_B`, `State_C` |
| `locality_class` | string | Urban/suburban/rural, drives home-cluster radius and the Day 2 fairness audit | urban 54,205 / suburban 47,009 / rural 35,866 |
| `issuance_day` | int | Day of month (1–28) this household's benefit is issued | 1–28 |
| `days_since_issuance` | float | Days elapsed since this household's current benefit cycle started, at time of transaction | 0–~30 |
| `monthly_benefit_amount` | float, USD | Household's fixed monthly benefit | clipped to [50, 900] |
| `remaining_balance_at_transaction` | float, USD | Point-in-time balance *before* this transaction: the household's benefit minus its own actual prior spend in the same issuance-aligned cycle, floored at 0. Added 2026-07-27 so the spend-baseline sub-score can be recomputed exactly from published columns rather than reconstructed — see EXPLAIN doc | min 0.00, median 93.04, mean 130.84, max 900.00; 1,798 rows at 0.00 (exhausted balance) |
| `is_fraud` | bool | Ground-truth label. **P1 is never `True`** — see EXPLAIN doc | True 547 / False 136,533 |
| `fraud_pattern` | string | `P2`–`P8`, `N1`, `N2`, or empty for ordinary rows. **No `P1` value exists in this column** | see breakdown below |
| `n1_crossing_issuance` | bool | For N1 rows only: whether this technical failure's store-and-forward window crossed into a new issuance period (7 CFR 274.12(m)) | True 48 / False (rest) |
| `terminal_compromised_at_time` | bool | Ground truth: was this row's terminal mid-compromise when the transaction happened, regardless of `is_fraud`. This is how P1 exposure is measured — see EXPLAIN doc | True 385 |

## `fraud_pattern` breakdown (measured)

| Value | Rows | Meaning |
|---|---|---|
| *(empty)* | 134,508 | Ordinary legitimate primary-user transaction |
| `N2` | 1,686 | Legitimate secondary/household-member use, `is_fraud=False` |
| `N1` | 339 | Technical failure (non-reversed residue only), `is_fraud=False` |
| `P6` | 82 | Terminal clustering — batch cashout through one terminal, `is_fraud=True` |
| `P3` | 78 | Balance probe immediately before drain, `is_fraud=True` |
| `P2` | 78 | Small test transaction before drain, `is_fraud=True` |
| `P8` | 78 | Repeated PIN-guess attempts before drain, `is_fraud=True` |
| `P5` | 77 | Out-of-state drain, `is_fraud=True` |
| `P4` | 77 | Issuance-day fast drain, `is_fraud=True` |
| `P7` | 77 | Off-hours drain, `is_fraud=True` |

## N1 store-and-forward crossing-issuance subset — measured drawdown

The file contains 339 N1 transactions, of which 48 are store-and-forward cases
crossing an issuance boundary (`n1_crossing_issuance = True`), drawing a mean
68.4% of monthly benefit (median 71.1%, range 42.6–88.3%).

Recorded here because it is cited in the paper's N1 section and the paper is not
distributed with this repository. `src/n1_crossing_drawdown.py` recomputes all
three figures from this CSV into
[`outputs/n1_crossing_drawdown.md`](../outputs/n1_crossing_drawdown.md), parses
them back out of this paragraph, and exits non-zero if they have drifted — so
this is a checked claim, not a hand-measured one like the observed values in the
table above.

`P1` (skimmer harvest) does not appear as a `fraud_pattern` value by design —
it is represented instead through `terminal_compromised_at_time=True` on
ordinary legitimate rows. See EXPLAIN doc for why.

## Related datasets not distributed here

`data/ebt_synthetic_realistic.csv` (538 MB, 38,000 households, 2.6M rows) is
the robustness-run dataset, generated at realistic fraud prevalence (0.02%)
rather than the elevated prevalence of the primary file. It is excluded by
`.gitignore` and is not distributed. It regenerates from
`src/ebt_generator.py` with `config.yaml`'s `random_seed: 42`.
